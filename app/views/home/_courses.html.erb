<div class="courses">
<h2>Courses</h2>
    <% @courses.map do |course| %>
        <div class="course-item">
            <p><%= course.name %></p>
            <small><%= link_to 'View details', course_path(course),class:"ui primary button"%></small>
        </div>
    <% end %>

</div>